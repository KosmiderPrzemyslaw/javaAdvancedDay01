import Dzień_1_Klasy_abstrakcyjne.Car;

public class Test extends Car {
    public Test(int maxSpeed, String model, String type) {
        super(maxSpeed, model, type);
    }

    Car car = new Car(12, "av", "toy");

   }
